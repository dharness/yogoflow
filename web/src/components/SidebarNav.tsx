import styled from "styled-components";

const StyledSidebarNav = styled.div`
  background: tan;
  grid-row: 1/3;
  grid-column: 1/2;
`;

const SidebarNav = () => {
  return <StyledSidebarNav />;
};

export default SidebarNav;
