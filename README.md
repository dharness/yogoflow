# yogoflow

Stay fit during those long work days with a guided exercise routine.

### Local Development:

Frontend:

```
cd web
npm install
npm run dev
```

Server:

```
cd server
pip install -r requirements.txt
python app.py
```
